// Store the famous person's name
const famous_person: string = "Woodrow Wilson"; 

// Compose the message with string interpolation
const message: string = `${famous_person} once said, "Friendship is the only cement that will ever hold the world together."`;

// Print the message
console.log(message);
