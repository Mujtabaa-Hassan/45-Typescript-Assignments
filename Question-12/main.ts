let friends: string[] = ["Jawad", "Saim", "Waqas"]; 
for (let i = 0; i < friends.length; i++) {
  const friendName: string = friends[i]; 
  console.log(`Hello, ${friendName}! How are you doing today?`);
}
